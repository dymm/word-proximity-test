spring.application.name=spring-boot-app
server.port=8080

# Logging
logging.level.root=INFO
logging.level.com.example=DEBUG

# Embedding Service - set to true when model files are available
embedding.enabled=true
